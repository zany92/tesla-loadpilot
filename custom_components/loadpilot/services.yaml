# Service descriptions — display strings live in translations/ (UX-owned).
set_bias:
  fields:
    amps:
      required: true
      example: 4.0
      selector:
        number:
          min: 0
          max: 16
          step: 0.5
          unit_of_measurement: "A"
          mode: slider

pause: {}

resume: {}
